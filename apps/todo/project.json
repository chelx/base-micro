{
  "name": "todo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/todo/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": [
          "--node-env=production"
        ],
        "cwd": "apps/todo"
      },
      "configurations": {
        "development": {
          "args": [
            "--node-env=development"
          ]
        }
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": [
        "build"
      ],
      "options": {
        "buildTarget": "todo:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "todo:build:development"
        },
        "production": {
          "buildTarget": "todo:build:production"
        }
      }
    },
    "db:typeorm": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx ts-node -P apps/todo/tsconfig.app.json -r tsconfig-paths/register ./node_modules/typeorm/cli.js",
        "cwd": "."
      }
    },
    "migration:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx nx db:typeorm todo -- migration:generate apps/todo/src/database/migrations/{args.name} -d apps/todo/src/database/data-source.ts",
        "cwd": "."
      }
    },
    "migration:run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx nx db:typeorm todo -- migration:run -d apps/todo/src/database/data-source.ts",
        "cwd": "."
      }
    },
    "migration:revert": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx nx db:typeorm todo -- migration:revert -d apps/todo/src/database/data-source.ts",
        "cwd": "."
      }
    }
  },
  "tags": []
}